# 2026-04-04 - Daily Notes 🦞

## ComfyUI + Wan2.1 T2V Setup Session (凌晨 1:03 AM - 3:10 AM)

### ✅ Completed Tasks

#### 1. **ComfyUI Server Startup**
- User manually started ComfyUI server with command: `cd ~/comfyui_install && source .venv/bin/activate && python main.py --listen --port 8188`
- Successfully opened UI in browser at http://localhost:8188

#### 2. **WanVideoWrapper Custom Nodes Installation** (earlier session)
- Installed WanVideoWrapper to `~/comfyui_install/custom_nodes/ComfyUI-WanVideo/` (~500KB, 10 Python files)
- Downloaded T5 Encoder Model: `models_t5_umt5-xxl-enc-bf16.pth` (~11GB)  
- Downloaded VAE Decoder: `Wan2.1_VAE.pth` (~480MB)

#### 3. **Workflow Creation Attempts**
- Attempted to create Wan2.1 T2V workflow JSON multiple times
- Issues encountered with tool calls (curl, exec commands frequently truncated/cut off mid-execution)
- User provided example file: `~/Downloads/ceshi.json` for reference

#### 4. **Final Workflow File**
- Successfully copied user's working JSON to workspace: `wan_t2v_from_ui.json`  
- Location: `~/.openclaw/workspace/comfyui-harness/wan_t2v_from_ui.json` or should be in ComfyUI workflows folder

### ❌ Issues & Learnings

#### Tool Call Problems
1. **curl + echo combinations** frequently truncated during exec execution
2. **tavily_search + tavily_extract** also prone to interruption  
3. **Repeated attempts caused infinite loops** - same command sent multiple times when output was cut off
4. User correctly identified: "我发现你无限循环了" (I found you're in an infinite loop)

#### ComfyUI Workflow Understanding Gap
1. Assistant admitted lack of understanding about Wan2.1 subgraph structure
2. Initial workflow attempts were incomplete/incorrect  
3. Realized need to study from user's working example (`ceshi.json`) instead of guessing format
4. User decided: "算了，等我自己研究明白再说吧" (Forget it, I'll figure this out myself first)

### 📝 Key Decisions & Context

1. **Environment Setup**  
   - Working directory: `~/comfyui_install`
   - Virtual env script: `.venv/bin/activate`
   - Server port: 8188 (listening mode enabled for remote access)

2. **Memory System Realization**  
   - User pointed out missing MEMORY.md file
   - Learned memory system structure from AGENTS.md:
     - `memory/YYYY-MM-DD.md`: Raw logs of daily events
     - `MEMORY.md`: Curated long-term wisdom (review periodically, not every session)

3. **Communication Pattern Change**  
   - User requested text-only replies when tool calls fail repeatedly
   - Agreed to avoid repeated tool attempts that cause loops

### 💡 Lessons Learned for Future Sessions

1. **Tool Call Reliability**: Some exec commands are unstable with certain output patterns (curl, large JSON)
2. **ComfyUI Workflow Structure**: Need proper understanding of subgraph format before creating workflows
3. **Memory System**: Should proactively check/create daily notes at session start in main sessions
4. **User Communication**: When user says "stop" or shows frustration, immediately switch to text-only mode

### 🎯 Next Steps (for future)

- [ ] Study ComfyUI Wan2.1 subgraph structure from `ceshi.json` example  
- [ ] Create proper workflow with complete node graph
- [ ] Test video generation once user has working JSON template
- [ ] Review and update MEMORY.md after several days of sessions

---

*Session ended at 3:10 AM PDT - User decided to study ComfyUI workflows independently first.* 🦞
