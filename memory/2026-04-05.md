# 2026-04-05 - Daily Log

## macos-control-mcp Installation Issue (18:03 PM)

**Problem:** User installed `macos-control-mcp` MCP server, which caused infinite loop during testing. Session had to be cleared multiple times due to tool output truncation bug in OpenClaw Control UI.

### Root Cause
- The skill was initially placed in `/Users/ling/.openclaw/workspace/skills/macos-use-server/` (wrong location)
- MCP server needs to be registered as a plugin via `plugins.entries.macos-control.config.command = "npx"` with args `["-y", "macos-control-mcp@latest"]`

### Actions Taken
1. Moved folder from skills directory → extensions: `/Users/ling/.openclaw/extensions/macos-control-mcp/` (fixed typo from macros to macos)
2. User manually configured MCP server in OpenClaw config with proper npx command
3. Attempted verification via `npx -y macos-control-mcp --version` but commands kept timing out

### Known Issues Discovered
1. **OpenClaw Control UI Bug:** Tool output > ~50 lines causes automatic session disconnection
2. **config.patch API issues:** Requires full JSON payload, partial updates fail silently or cause errors
3. **MCP server startup delay:** npx commands can hang for 8-10 seconds before responding

### Current Status (7:15 PM)
User manually configured `macos-control` MCP plugin in OpenClaw config. Verification pending - need to test if it loads correctly without triggering session disconnects due to tool output limits.

**Recommendation:** Test with minimal commands first, avoid verbose outputs that trigger Control UI disconnection bug.
